<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sdkdroid.fime_just_one">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_fime_foreground"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CompanyProfileActivity"></activity>
        <activity android:name=".LaporanActivity" />
        <activity android:name=".EditInputanActivity" />
        <activity android:name=".InputDataActivity" />
        <activity android:name=".PemasukanPengeluaranActivity" />
        <activity android:name=".UpdatePerusahaanActivity" />
        <activity android:name=".PerusahaanActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>